apiVersion: v1
kind: Service
metadata:
  name: {{ include "nutrition-api.fullname" . }}
  labels:
    {{- include "nutrition-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nutrition-api.selectorLabels" . | nindent 4 }}
